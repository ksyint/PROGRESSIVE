# Mode: 'vqa' for generating 3-stage VQA data, 'caption' 
mode: vqa

model:
  huatuo_path: "/path/to/your/HuatuoGPT-Vision-34B"

data:
  input_json: "data/input_detection.json"
  output_vqa: "output/vqa_cot_data.json"

processing:
   # Threshold to binarize organ masks before getting their bounding box
  mask_threshold: 127
